/** @type {import('next').NextConfig} */
const nextConfig = {};

export default {
  images: {
    domains: [
      'lh3.googleusercontent.com',
      // puedes agregar otros dominios aquí si usas más proveedores
    ],
  },
};
